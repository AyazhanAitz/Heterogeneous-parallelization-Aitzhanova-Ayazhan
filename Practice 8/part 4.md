# **Задание 4. Анализ производительности**

## 1. Сравнение времени выполнения на CPU, GPU и в гибридном режиме

В рамках работы было проведено сравнение времени обработки массива данных в трёх режимах:

* на **CPU с использованием OpenMP**,
* на **GPU с использованием CUDA**,
* в **гибридном режиме**, где вычисления выполняются одновременно на CPU и GPU.

Результаты показали, что **CPU-реализация** обладает наибольшим временем выполнения, так как, несмотря на использование многопоточности, количество вычислительных ресурсов ограничено числом ядер процессора.

**GPU-реализация** обеспечивает ускорение за счёт массового параллелизма и большого количества потоков. Однако итоговое время выполнения включает не только вычисления, но и затраты на передачу данных между CPU и GPU, что снижает эффективность при простых операциях.

**Гибридная реализация** демонстрирует наилучшие результаты за счёт одновременного выполнения вычислений на CPU и GPU, что позволяет сократить общее время обработки массива.

## 2. Анализ эффективности гибридного подхода

Гибридный подход показывает наибольший выигрыш в производительности в следующих случаях:

* при **большом размере массива данных**;
* при возможности **параллельного выполнения** вычислений на CPU и GPU;
* при использовании **асинхронных операций**, позволяющих перекрывать вычисления CPU с передачей данных и вычислениями на GPU;
* при вычислительно более сложных операциях, где доля вычислений превышает накладные расходы на передачу данных.

При простых операциях (например, умножение элемента массива на константу) эффективность гибридного подхода может быть ограничена из-за доминирования затрат на копирование данных между CPU и GPU. Тем не менее, с увеличением сложности вычислений и объёма данных гибридный подход становится более эффективным по сравнению с использованием только CPU или только GPU.

## **Вывод**

Гибридная обработка массива позволяет наиболее эффективно использовать вычислительные ресурсы системы за счёт одновременной работы CPU и GPU. Наибольший выигрыш гибридный подход демонстрирует при больших объёмах данных и возможности перекрытия вычислений и передачи данных.

Скажи, как у вас требуют — сделаем идеально 👌
