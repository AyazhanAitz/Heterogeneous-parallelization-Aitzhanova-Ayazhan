
## **1. Какие основные типы памяти используются в OpenCL?**

В OpenCL используется несколько уровней памяти, каждый из которых предназначен для определённых задач и имеет свои характеристики по скорости и области видимости.

### 🔹 **Глобальная память (`__global`)**

Глобальная память доступна **всем рабочим элементам (work-items)** и используется для хранения входных и выходных данных, таких как массивы и матрицы. Она имеет **большую ёмкость**, но **высокую задержку доступа**.

**Пример:**
Матрицы `A`, `B` и `C` в задаче умножения матриц хранятся в глобальной памяти.

```c
__global const float* A;
__global float* C;
```

---

### 🔹 **Локальная память (`__local`)**

Локальная память общая для **всех рабочих элементов внутри одной рабочей группы (work-group)**. Она значительно быстрее глобальной памяти и часто используется для оптимизации доступа к данным.

**Пример:**
При оптимизированном умножении матриц часть матриц копируется в локальную память:

```c
__local float tileA[16][16];
__local float tileB[16][16];
```

---

### 🔹 **Частная память (`__private`)**

Частная память принадлежит **одному рабочему элементу** и обычно хранится в регистрах. Она самая быстрая, но имеет ограниченный размер.

**Пример:**
Переменная `sum` внутри ядра:

```c
float sum = 0.0f;
```

---

### 🔹 **Константная память (`__constant`)**

Константная память доступна всем рабочим элементам **только для чтения** и кэшируется. Она эффективна, если все потоки читают одни и те же данные.

**Пример:**
Передача неизменяемых параметров или коэффициентов:

```c
__constant float coeffs[10];
```

---

### 🔹 **Память изображений (Image Memory)**

Используется для работы с изображениями и текстурами, имеет специализированное аппаратное кэширование.

**Пример:**
Обработка изображения:

```c
read_imagef(image, sampler, coord);
```

---

## **2. Как настроить глобальную и локальную рабочую группу?**

В OpenCL вычисления организованы в виде **иерархии**:
**глобальное пространство → рабочие группы → рабочие элементы**.

### 🔹 **Глобальная рабочая группа (Global Work Size)**

Определяет **общее количество рабочих элементов**, которые будут запущены. Обычно соответствует размеру обрабатываемых данных.

**Пример:**
Для матрицы `C` размером `N×K`:

```cpp
size_t globalSize[2] = { (size_t)N, (size_t)K };
```

---

### 🔹 **Локальная рабочая группа (Local Work Size)**

Определяет, **сколько рабочих элементов объединяются в одну группу**. Размер выбирается так, чтобы эффективно использовать локальную память и вычислительные ресурсы устройства.

**Пример:**
Тайлинг 16×16:

```cpp
size_t localSize[2] = { 16, 16 };
```

---

### 🔹 **Связь глобального и локального размеров**

Глобальный размер должен быть **кратен локальному**, либо в ядре должна быть проверка границ:

```c
if (row < N && col < K) { ... }
```

---

## **3. Чем отличается OpenCL от CUDA?**

### 🔹 **CUDA**

* Разработана компанией **NVIDIA**
* Работает **только на GPU NVIDIA**
* Предоставляет более низкоуровневый доступ к аппаратуре
* Часто показывает более высокую производительность на NVIDIA GPU

**Пример:**
CUDA ядро:

```cpp
__global__ void add(float* A, float* B, float* C) {
    int id = blockIdx.x * blockDim.x + threadIdx.x;
    C[id] = A[id] + B[id];
}
```

---

### 🔹 **OpenCL**

* **Открытый стандарт**, поддерживаемый разными производителями
* Работает на **CPU, GPU, FPGA, DSP**
* Один и тот же код можно запускать на разных устройствах
* Немного сложнее в настройке

**Пример:**
OpenCL ядро:

```c
__kernel void add(__global float* A, __global float* B, __global float* C) {
    int id = get_global_id(0);
    C[id] = A[id] + B[id];
}
```

---

### 🔹 **Ключевое отличие**

CUDA — это **максимальная производительность на NVIDIA**,
OpenCL — **переносимость и универсальность**.

---

## **4. Какие преимущества дает использование OpenCL?**

### 🔹 **Кросс-платформенность**

Один и тот же OpenCL-код можно запускать на CPU и GPU разных производителей без переписывания алгоритма.

**Пример:**
Одна программа может работать:

* на CPU Intel
* на GPU NVIDIA (Tesla T4)
* на GPU AMD

---

### 🔹 **Гетерогенные вычисления**

OpenCL позволяет использовать **все вычислительные ресурсы системы** одновременно.

**Пример:**
CPU — подготовка данных,
GPU — тяжёлые параллельные вычисления.

---

### 🔹 **Масштабируемость**

Программа автоматически масштабируется под доступное устройство, используя тысячи потоков.

---

### 🔹 **Гибкость**

OpenCL даёт контроль над:

* памятью,
* рабочими группами,
* синхронизацией.
